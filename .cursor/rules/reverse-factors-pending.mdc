---
description: 待实现的"反向指标"因子设计(竞彩赔率迎合共识+单关→反向;F3实力定位逆向信号)
globs: api-service/predict_service.py
---

# 待实现:反向指标因子(用户多年实战经验)

## 反向指标元规则(2026-06-05 提出,待实现)
当 **单关** + **多因子方向共识** + **竞彩胜平负赔率迎合该共识** 时,大概率触发**反向**。
- 例1:主队上盘,所有因素看好主队(上盘),主队竞彩主胜赔率下降(迎合),且单关 → 极大概率客队(下盘)赢盘。
- 例2:所有因素看好客队(下盘),主队竞彩主胜赔率上升(迎合),且单关 → 极大概率主队(上盘)硬盘。
- 本质:"群众的反面"——散户压一边+竞彩赔率顺应+单关吸量,庄家做反向。

## 数据可得性
- 竞彩胜平负(had)赔率变动能实时从 500 网抓:`fetch_euro_history(fid, cid=1)`,cid=1 是竞彩官方,返回初盘到最新完整序列(已验证)。预测时实时抓即可,不依赖 DB 存储。
- 历史 jczq_odds_history 表只有 spf/nspf 变动(已回采 spf),历史回测此规则受限。

## 落地方案(用户确认)
1. 预测时实时抓 fetch_euro_history 实现反向因子(F6 升级或新增因子)。
2. scraper-service 每次同步把竞彩 had 当前赔率存成带时间戳快照留档(scraper 用体彩 API,build_had_hhad 返回 had,存快照即可,不需 500 fid)。

## F3 实力定位的逆向信号(2026-06-08 确认方向,待实现)
F3 当前:权重 1.0,量化排名对比 + AI 底蕴判断(3 次投票),输出"大众认为谁更强"。
**核心问题**:让球方几乎总是实力更强方,F3 正常加权产生固定上盘偏移。
**设计**:F3 作逆向信号/置信度调节器,而非正向加权因子。
**触发(全部满足)**:
1. F3 判断上盘实力明显占优(排名差≥5 或 AI 2/3 认为上盘强)。
2. 其他因子≥3 个 direction=upper(上盘共识)。
3. 盘口绝对值 ≤1.0 球(深盘不触发,庄家已用盘口保护下盘)。
**效果**:F3 direction 翻转为 lower(score 6-7)参与加权,降低"一边倒 upper"的过度自信,reason "上盘共识过强,逆向修正"。未触发时 F3 neutral 不参与方向加权。
**实现**:修改 calc_prediction 综合计算,检测 F3+其他因子共识后决定是否翻转。用户计划增加更多因子后统一实现逆向机制。

## 中断点
曾准备在 scraper-service/schema_mysql.sql 加 had_odds_history 表时被叫停去提交部署。下次可从这里继续。
